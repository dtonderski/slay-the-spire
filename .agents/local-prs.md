# Local PR Queue

Integrator branch: `codex/integration-m32a`

Use this file to track parallel worker branches before integration. Each worker should own a narrow scope, avoid unrelated files, and report tests run.

| Branch | Owner | Scope | Expected write set | Status | Tests | Merge notes |
| --- | --- | --- | --- | --- | --- | --- |
| `codex/integration-m32a` | integrator | Stabilize current working tree and merge reviewed worker branches | repo-wide as needed for integration only | active | pending | Current dirty tree moved from `master` onto this branch. |
| audit/cards | Lagrange | Read-only M32A audit of cards, statuses, curses, mappings, tests, and evidence | none | completed | n/a | Recommends one row per `ContentId`; separate trace mapping from effect modeling. |
| audit/relics-potions | Dewey | Read-only M32A audit of relic/potion mappings, pools, tests, and evidence | none | completed | n/a | Recommends one row per `RelicKey`/`Relic`/`Potion`; split proof strength by row. |
| audit/run-world | Descartes | Read-only M32A audit of monsters, bosses, encounters, events, rooms, rewards, shops, rest, map, ascension, and corpus coverage | none | completed | n/a | Recommends run/world rows by surface group and explicit RNG stream/caveat columns. |
| `codex/m32a-matrix-shell` | integrator | Add matrix vocabulary and empty schema row | `simulator/docs/content_support_matrix.md`, `.agents/local-prs.md` | ready for commit | docs only | Base artifact for future matrix workers. |
| `codex/m32a-coverage-fixture` | integrator | Add a failing fixture for missing M32A inventory rows and close roadmap status | `simulator/crates/sts_core/tests/m32a_matrix.rs`, matrix docs, `TASKS.md`, `STATUS.md` | integrated | `cargo test -p sts_core --test m32a_matrix` | Fixture covers cards, generated pool IDs, relics, potions, monsters, encounters, events, rooms, and named run-world anchors. |
| `codex/m32b-iron-wave` | integrator | Implement deterministic Iron Wave combat behavior | `simulator/crates/sts_core/src/content/cards.rs`, `simulator/crates/sts_core/src/combat/*`, matrix/status docs | integrated | `cargo test -p sts_core iron_wave`; `cargo fmt`; `cargo clippy`; `cargo test` | Base Iron Wave is local-tested only; no played-card trace parity claim. |
| `codex/m32b-body-slam` | integrator | Implement deterministic Body Slam combat behavior | `simulator/crates/sts_core/src/content/cards.rs`, `simulator/crates/sts_core/src/combat/*`, matrix/status docs | integrated | `cargo test -p sts_core body_slam`; `cargo fmt`; `cargo clippy`; `cargo test` | Base Body Slam is local-tested only; no played-card trace parity claim. |
| `codex/m32b-clash` | integrator | Implement deterministic Clash combat behavior | `simulator/crates/sts_core/src/content/cards.rs`, `simulator/crates/sts_core/src/combat/*`, matrix/status docs | integrated | `cargo test -p sts_core clash`; `cargo fmt`; `cargo clippy`; `cargo test` | Base Clash is local-tested only; no played-card trace parity claim. |
| `codex/m32b-wild-strike` | integrator | Implement deterministic Wild Strike combat behavior | `simulator/crates/sts_core/src/content/cards.rs`, `simulator/crates/sts_core/src/combat/*`, matrix/status docs | integrated | `cargo test -p sts_core wild_strike`; `cargo fmt`; `cargo clippy`; `cargo test` | Base Wild Strike is local-tested only; no played-card trace parity or exact Wound draw-pile shuffle-order claim. |
| `codex/m32b-heavy-blade` | integrator | Implement deterministic Heavy Blade combat behavior | `simulator/crates/sts_core/src/content/cards.rs`, `simulator/crates/sts_core/src/combat/*`, matrix/status docs | integrated | `cargo test -p sts_core heavy_blade`; `cargo fmt`; `cargo clippy`; `cargo test` | Base Heavy Blade is local-tested only; no played-card trace parity claim. |
| `codex/m32b-perfected-strike` | Laplace + integrator | Implement deterministic Perfected Strike combat behavior | `simulator/crates/sts_core/src/content/cards.rs`, `simulator/crates/sts_core/src/combat/*`, matrix/status docs | integrated | `cargo test -p sts_core perfected_strike`; `cargo fmt`; `cargo clippy`; `cargo test` | Base Perfected Strike is local-tested only; counts current combat piles rather than master deck; no played-card trace parity claim. |
| `codex/m32b-power-through` | Goodall + integrator | Implement deterministic Power Through combat behavior | `simulator/crates/sts_core/src/content/cards.rs`, `simulator/crates/sts_core/src/combat/*`, matrix/status docs | integrated | `cargo test -p sts_core power_through`; `cargo fmt`; `cargo clippy`; `cargo test` | Base Power Through is local-tested only; generated Wounds use local hand insertion semantics; no played-card trace parity claim. |
| `codex/m32b-reckless-charge` | Sagan + Linnaeus + integrator | Implement deterministic Reckless Charge combat behavior | `simulator/crates/sts_core/src/content/cards.rs`, `simulator/crates/sts_core/src/combat/*`, matrix/status docs | integrated | `cargo test -p sts_core reckless_charge`; `cargo fmt`; `cargo clippy`; `cargo test` | Base Reckless Charge is local-tested only; generated Dazed uses local draw-pile insertion semantics; no played-card trace parity or exact Dazed shuffle-order claim. |
| `codex/m32b-pummel` | Godel + Ampere + integrator | Implement deterministic Pummel combat behavior | `simulator/crates/sts_core/src/content/cards.rs`, `simulator/crates/sts_core/src/combat/*`, matrix/status docs | integrated | `cargo test -p sts_core pummel`; `cargo fmt`; `cargo clippy`; `cargo test` | Base Pummel is local-tested only; no played-card trace parity claim. |
| `codex/m32b-ghostly-armor` | Carver + Curie + integrator | Implement deterministic Ghostly Armor combat behavior | `simulator/crates/sts_core/src/content/cards.rs`, `simulator/crates/sts_core/src/combat/*`, matrix/status docs | integrated | `cargo test -p sts_core ghostly`; `cargo fmt`; `cargo clippy`; `cargo test` | Base Ghostly Armor is local-tested only; no played-card trace parity or exact ethereal timing claim. |
| `codex/m32b-bloodletting` | James + Leibniz + integrator | Implement deterministic Bloodletting combat behavior | `simulator/crates/sts_core/src/content/cards.rs`, `simulator/crates/sts_core/src/combat/*`, matrix/status docs | integrated | `cargo test -p sts_core bloodletting`; `cargo fmt`; `cargo clippy`; `cargo test` | Base Bloodletting is local-tested only; no played-card trace parity or decoded action-order claim. |
| `codex/m32b-carnage` | Archimedes + Russell + integrator | Implement deterministic Carnage combat behavior | `simulator/crates/sts_core/src/content/cards.rs`, `simulator/crates/sts_core/src/combat/*`, matrix/status docs | integrated | `cargo test -p sts_core carnage`; `cargo fmt`; `cargo clippy`; `cargo test` | Base Carnage is local-tested only; no played-card trace parity or exact ethereal timing claim. |
| `codex/m32b-hemokinesis` | Wegener + Einstein + integrator | Implement deterministic Hemokinesis combat behavior | `simulator/crates/sts_core/src/content/cards.rs`, `simulator/crates/sts_core/src/combat/*`, matrix/status docs | integrated | `cargo test -p sts_core hemokinesis`; `cargo fmt`; `cargo clippy`; `cargo test` | Base Hemokinesis is local-tested only; no played-card trace parity, decoded action-order, or Duplication Potion source timing claim. |
| `codex/m32b-sentinel` | Chandrasekhar + Volta + integrator | Implement deterministic Sentinel combat behavior | `simulator/crates/sts_core/src/content/cards.rs`, `simulator/crates/sts_core/src/combat/*`, matrix/status docs | integrated | `cargo test -p sts_core sentinel`; `cargo test -p sts_core exhaust`; `cargo fmt`; `cargo clippy`; `cargo test` | Base Sentinel is local-tested only; no played-card trace parity, source-backed exhaust-order, or upgraded Sentinel behavior claim. |
| `codex/m32b-doubt` | Parfit + Faraday + integrator | Implement deterministic Doubt end-turn curse behavior | `simulator/crates/sts_core/src/combat/hand.rs`, `simulator/crates/sts_core/src/combat/turn.rs`, matrix/status docs | integrated | `cargo test -p sts_core combat::hand::tests`; `cargo fmt`; `cargo clippy`; `cargo test` | Base Doubt is local-tested only; no real-game trace parity or source-backed ordering/duplicate-trigger claim. |
| `codex/m32b-wound` | Pauli + Bohr + integrator | Implement deterministic Wound inert status behavior | `simulator/crates/sts_core/src/content/cards.rs`, `simulator/crates/sts_core/src/combat/hand.rs`, matrix/status docs | integrated | `cargo test -p sts_core wound`; `cargo test -p sts_core medical_kit`; `cargo fmt`; `git diff --check`; `cargo clippy`; `cargo test` | Base Wound is local-tested only; no real-game trace parity or source-backed exact Wound timing claim. |
| `codex/m32b-regret` | Kepler + Planck + integrator | Implement deterministic Regret end-turn curse behavior | `simulator/crates/sts_core/src/content/cards.rs`, `simulator/crates/sts_core/src/combat/hand.rs`, matrix/status docs | integrated | `cargo test -p sts_core regret`; `cargo fmt`; `git diff --check`; `cargo clippy`; `cargo test` | Base Regret is local-tested only; no standalone real-game trace parity or source-backed exact end-turn timing claim. |
| `codex/m32b-dazed` | Bacon + McClintock + integrator | Implement deterministic Dazed ethereal cleanup behavior | `simulator/crates/sts_core/src/content/cards.rs`, `simulator/crates/sts_core/src/combat/hand.rs`, matrix/status docs | integrated | `cargo test -p sts_core dazed`; `cargo test -p sts_core combat::hand::tests`; `cargo fmt`; `cargo clippy`; `cargo test` | Base Dazed is local-tested only; no real-game trace parity or source-backed exact ethereal timing claim. |
| `codex/m32b-bludgeon` | integrator | Implement deterministic Bludgeon combat behavior | `simulator/crates/sts_core/src/content/cards.rs`, `simulator/crates/sts_core/src/combat/*`, matrix/status docs | integrated | `cargo test -p sts_core bludgeon`; `cargo fmt`; `cargo clippy`; `cargo test` | Base Bludgeon is local-tested only; no played-card trace parity claim. |
| `codex/m32b-impervious` | integrator | Implement deterministic Impervious combat behavior | `simulator/crates/sts_core/src/content/cards.rs`, `simulator/crates/sts_core/src/combat/*`, matrix/status docs | integrated | `cargo test -p sts_core impervious`; `cargo fmt`; `cargo clippy`; `cargo test` | Base Impervious is local-tested only; no played-card trace parity claim. |
| `codex/m32b-clothesline` | integrator | Implement deterministic Clothesline combat behavior | `simulator/crates/sts_core/src/content/cards.rs`, `simulator/crates/sts_core/src/combat/*`, matrix/status docs | integrated | `cargo test -p sts_core clothesline`; `cargo fmt`; `cargo clippy`; `cargo test` | Base Clothesline is local-tested only; no played-card trace parity claim. |
| `codex/m32b-intimidate` | integrator | Implement deterministic Intimidate combat behavior | `simulator/crates/sts_core/src/content/cards.rs`, `simulator/crates/sts_core/src/combat/*`, matrix/status docs | integrated | `cargo test -p sts_core intimidate`; `cargo fmt`; `cargo clippy`; `cargo test` | Base Intimidate is local-tested only; no played-card trace parity claim. |

## Recommended Next Local PRs

| Branch | Scope | Write set |
| --- | --- | --- |
| `codex/m32a-card-inventory` | Fill card, colorless, curse, and status rows. | `simulator/docs/m32a_cards_matrix.md` only. |
| `codex/m32a-relic-potion-inventory` | Fill relic and potion rows. | `simulator/docs/m32a_relic_potion_matrix.md` only. |
| `codex/m32a-run-world-inventory` | Fill monster, boss, encounter, event, room, reward, shop, rest, map, ascension, verifier, and corpus rows. | `simulator/docs/m32a_run_world_matrix.md` only. |

## Active Workers

| Agent | Branch equivalent | Scope | Write set | Status |
| --- | --- | --- | --- | --- |
| Poincare | `codex/m32a-card-inventory` | Fill card inventory matrix rows. | `simulator/docs/m32a_cards_matrix.md` | completed |
| Galileo | `codex/m32a-relic-potion-inventory` | Fill relic/potion inventory matrix rows. | `simulator/docs/m32a_relic_potion_matrix.md` | completed, integrated with Fairy caveat correction |
| Averroes | `codex/m32a-run-world-inventory` | Fill run/world inventory matrix rows. | `simulator/docs/m32a_run_world_matrix.md` | completed |

## Worker Contract

- Start from the current integration branch checkpoint.
- Own a disjoint write set where possible.
- Do not revert unrelated edits.
- Keep changes small and reviewable.
- Run focused tests plus the project gate when feasible: `cargo fmt`, `cargo clippy`, and `cargo test` from `simulator/`.
- Final report must include changed files, diff summary, tests run, and known risks.
