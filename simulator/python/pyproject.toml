[build-system]
requires = ["maturin>=1.7,<2"]
build-backend = "maturin"

[project]
name = "sts-sim"
version = "0.1.0"
description = "Typed Python bindings for the Slay the Spire simulator"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
sts-combat-data = "sts_sim.rl.cli:data_main"
sts-combat-train = "sts_sim.rl.cli:train_main"
sts-combat-evaluate = "sts_sim.rl.cli:evaluate_main"

[project.optional-dependencies]
rl = [
    "numpy>=2",
    "torch>=2.7",
]

[dependency-groups]
dev = [
    "jupyterlab>=4.0",
    "maturin>=1.7,<2",
    "pytest>=8.0",
    "ruff>=0.12",
    "ty>=0.0.1",
]

[tool.maturin]
manifest-path = "../../bindings/py_sts/Cargo.toml"
python-source = "."
module-name = "sts_sim._native"
features = ["extension-module"]

[tool.ty.environment]
python-version = "3.12"

[tool.ty.src]
include = ["sts_sim", "tests"]

[tool.ty.rules]
missing-type-argument = "error"
possibly-unresolved-reference = "warn"
blanket-ignore-comment = "error"

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
extend-select = ["ANN", "PYI", "PGH003"]
preview = true
